# Configuration NextJS (App Router)

1) Adapt tsconfig.js to resolve path wite "baseurl" and "paths" options.
2) Use `use client` on the top on a fil if you wanna use CSR (Client Side Rendering) instead of SSR (Server Side Rendering).
https://nextjs.org/docs/app/building-your-application/rendering/client-components

3) 